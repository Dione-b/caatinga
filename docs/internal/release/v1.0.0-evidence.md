# v1.0.0 Release Evidence

Git tag `v1.0.0` marks the **stable contract milestone**. npm packages ship as `@caatinga/*@3.8.0` with `latest` dist-tag.

## Verification (2026-07-06)

| Gate                | Command                               | Result                                       |
| ------------------- | ------------------------------------- | -------------------------------------------- |
| Unit tests          | `pnpm test`                           | pass                                         |
| Compatibility       | `pnpm test:compat`                    | pass                                         |
| Typecheck           | `pnpm typecheck`                      | pass                                         |
| Public API manifest | `docs/public-api.md`                  | published                                    |
| RC checklist        | `docs/release-candidate-checklist.md` | complete (smoke CI operational gate ongoing) |

## Publish

```bash
pnpm ci:publish-matrix   # full gate
pnpm pre:publish           # publish @caatinga/* with --tag latest
```

## Versioning policy

- **v1.0.0** = git contract tag
- **3.8.0** = npm semver (major `3.x` continues)
- Breaking Tier 1 changes require `4.0.0`
